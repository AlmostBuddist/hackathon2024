# hackathon2024-server

## Installing packages

```bash
pip install -r requirements.txt
```

## Start

Run the command:

```bash
python -m flask run
```

## Debug mode: on

```bash
python -m flask run --debug
```
